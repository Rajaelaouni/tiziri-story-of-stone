import { createFileRoute } from "@tanstack/react-router"; import { useState } from "react"; import { z } from "zod"; import landscape from "@/assets/ourika-landscape.jpg"; import { Button } from "@/components/ui/button"; const schema=z.object({nom:z.string().trim().min(2).max(100),email:z.string().trim().email().max(255),sujet:z.string().trim().min(2).max(150),message:z.string().trim().min(10).max(1000)});export const Route=createFileRoute("/contact")({head:()=>({meta:[{title:"Contact — TIZIRI"},{name:"description",content:"Échangez avec la maison TIZIRI au sujet d’une création artisanale."},{property:"og:title",content:"Contact — TIZIRI"},{property:"og:description",content:"Parlons de votre histoire."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}],links:[{rel:"canonical",href:"/contact"}]}),component:Page});function Page(){const [sent,setSent]=useState(false);const submit=(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget));if(schema.safeParse(data).success){setSent(true);e.currentTarget.reset()}else e.currentTarget.reportValidity()};return <><section className="relative min-h-[48svh] pt-36 text-hero-foreground"><img src={landscape} alt="Lumière sur la vallée d’Ourika" className="absolute inset-0 h-full w-full object-cover"/><div className="absolute inset-0 bg-ink/55"/><div className="relative mx-auto max-w-7xl px-6"><h1 className="display-title text-5xl md:text-8xl">Parlons de votre histoire.</h1><p className="mt-6">Une question sur une création ? Une envie particulière ? Écrivez-nous.</p></div></section><section className="px-6 py-24"><div className="mx-auto grid max-w-6xl gap-16 md:grid-cols-[1.4fr_.6fr]"><form onSubmit={submit} className="grid gap-8"><label className="field-label">Nom<input name="nom" required minLength={2}/></label><label className="field-label">Email<input name="email" type="email" required/></label><label className="field-label">Sujet<input name="sujet" required/></label><label className="field-label">Message<textarea name="message" required minLength={10} rows={5}/></label><Button variant="luxury" className="w-fit" type="submit">Envoyer →</Button>{sent&&<p role="status" className="text-sm text-forest">Merci. Votre message est prêt à être transmis à la maison TIZIRI.</p>}</form><aside><h2 className="font-serif text-4xl">TIZIRI</h2><p className="mt-2 text-sm">Créations d’Ahmed Tiziri</p><p className="mt-8 text-sm">Ourika — Maroc</p></aside></div></section></>}
